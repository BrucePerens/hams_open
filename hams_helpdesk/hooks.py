import os

def post_init_hook(env):
    # Tested by [@ANCHOR: helpdesk_doc_injection]
    # Verified by [@ANCHOR: test_05_doc_injection]
    # Odoo automatically calls _register_hook on models when the registry is ready.
    pass
