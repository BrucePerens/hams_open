<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="pager_duty_procedures" model="ir.actions.server">
            <!-- [@ANCHOR: pager_duty_postgres_procedures] -->

            <!-- Verified by [@ANCHOR: test_pager_duty_procedures] -->
            <field name="name">Pager Duty: Install Postgres Procedures</field>
            <field name="model_id" ref="base.model_ir_actions_server"/>
            <field name="state">code</field>
            <field name="code">
# Postgres procedures are installed by
# hooks._install_postgres_procedures()
# which wraps cr.execute() in a savepoint.
log("Pager Duty procedures installed via hook.")
            </field>
        </record>
    </data>
</odoo>
