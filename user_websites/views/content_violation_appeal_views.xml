<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
<odoo>
        <record id="view_content_violation_appeal_list" model="ir.ui.view"> 
        <!-- audit-ignore-view: Tested by [@ANCHOR: test_user_websites_backend_views_rendering] -->
        <field name="name">content.violation.appeal.list</field>
        <field name="model">content.violation.appeal</field>
        <field name="arch" type="xml">
            <list string="Moderation Appeals" decoration-danger="state=='new'" decoration-success="state=='approved'" decoration-muted="state=='rejected'">
                <field name="create_date"/>
                <field name="user_id"/>
                <field name="group_id"/>
                <field name="state" widget="badge" 
                       decoration-danger="state=='new'" 
                       decoration-success="state=='approved'" 
                       decoration-muted="state=='rejected'"/>
            </list>
        </field>
    </record>

        <record id="view_content_violation_appeal_form" model="ir.ui.view"> 
        <!-- audit-ignore-view: Tested by [@ANCHOR: test_user_websites_backend_views_rendering] -->
        <field name="name">content.violation.appeal.form</field>
        <field name="model">content.violation.appeal</field>
        <field name="arch" type="xml">
            <form string="Moderation Appeal">
                <header>
                    <button name="action_approve" string="Approve &amp; Pardon" type="object" class="oe_highlight" invisible="not id or state != 'new'" confirm="This will lift the user's suspension immediately. Continue?" groups="user_websites.group_user_websites_administrator"/>
                    <button name="action_reject" string="Reject Appeal" type="object" class="btn-danger" invisible="not id or state != 'new'" groups="user_websites.group_user_websites_administrator"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,approved,rejected"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="user_id" readonly="1" invisible="not user_id"/>
                            <field name="group_id" readonly="1" invisible="not group_id"/>
                        </h1>
                    </div>
                    <group>
                        <field name="create_date" readonly="1"/>
                        <field name="reason" readonly="1"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="action_content_violation_appeal" model="ir.actions.act_window">
        <field name="name">Moderation Appeals</field>
        <field name="res_model">content.violation.appeal</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('state', '=', 'new')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No active appeals from suspended users or groups.
            </p>
        </field>
    </record>

    <menuitem id="menu_content_violation_appeal"
              name="Appeals"
              parent="menu_user_websites_root"
              action="action_content_violation_appeal"
              groups="user_websites.group_user_websites_administrator"
              sequence="35"/>

    <record id="action_my_content_violation_appeal" model="ir.actions.act_window">
        <field name="name">My Moderation Appeals</field>
        <field name="res_model">content.violation.appeal</field>
        <field name="view_mode">list,form</field>
        <field name="domain">['|', ('user_id', '=', uid), ('group_id.member_ids', 'in', [uid])]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                You have not submitted any appeals.
            </p>
        </field>
    </record>

    <menuitem id="menu_my_content_violation_appeal"
              name="My Appeals"
              parent="menu_user_websites_root"
              action="action_my_content_violation_appeal"
              groups="base.group_portal"
              sequence="36"/>
</odoo>

