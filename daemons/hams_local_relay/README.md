# Hams Local Relay

This directory holds the code and dependencies for the local relay component.

### Functions
- Currently contains the `radae` submodule/external code directory.
- `radae` is an external dependency; its contents are managed separately and should not be modified by automated assistants.
